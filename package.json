{
  "name": "gacha-mr-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "cross-env DOTENV_CONFIG_PATH=.env.dev tsx watch src/bot.ts",
    "dev:deploy": "cross-env DOTENV_CONFIG_PATH=.env.dev tsx src/deploy-commands.ts",
    "dev:migrate": "cross-env DOTENV_CONFIG_PATH=.env.dev drizzle-kit migrate",
    "dev:ingest": "cross-env DOTENV_CONFIG_PATH=.env.dev tsx scripts/ingest-wiki.ts",
    "live:dev": "tsx watch src/bot.ts",
    "start": "node dist/src/index.js",
    "start:single": "node dist/src/start.js",
    "migrate": "node dist/src/migrate.js",
    "build": "tsc",
    "deploy-commands": "tsx src/deploy-commands.ts",
    "ingest": "tsx scripts/ingest-wiki.ts",
    "ingest:api": "tsx scripts/ingest.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "test": "tsx --test tests/concurrency.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "cross-env": "^10.1.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
